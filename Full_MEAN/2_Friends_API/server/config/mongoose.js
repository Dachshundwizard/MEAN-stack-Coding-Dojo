console.log('loading mongoose connection and model...');
var mongoose = require('mongoose');
var fs = require('fs'); // FS module provides ability to read and write files. Reading is how we obtain the content to serve the clients, and writing is how we output content to the client. 
